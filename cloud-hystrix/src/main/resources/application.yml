eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
server:
  port: 8766
spring:
  application:
    name: cloud-hystric
#下面这个配置必须弄成这样
feign.hystrix.enabled: true
#feign:
#  hystrix:
#    enabled: true

